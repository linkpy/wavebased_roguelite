// @description

motion_direction = random_range(0, 360);
motion_speed = random_range(200, 400);
motion_reduction_factor = 0.06;

max_lifetime = 10;
lifetime = max_lifetime;

collision_script = noone;